@model List<GeneralModel<SkillProfi.Project.Project>>

@{
    ViewData["Title"] = "Projects";
}

<div class="title">
    <a>Projects</a>
</div>

<div id="projectScroller" class="scroller">

    @{ 
    foreach (var proj in Model)
    {
        <div class="block">
            <a href="@Url.Action("Project", "Projects", new{id = proj.Object.Id})">
                <div class="block-image">       
                     <img src='@(proj.ImageLink)' onerror="this.src='/Assets/ImageError.png'">
                </div>
                <div class="block-title-in">
                    <p>@proj.Object.Title</p>
                </div>
            </a>
        </div>
    }
    }
</div>
