<Window x:Class="SkillProfiWPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SkillProfiWPF"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800"
        Background="#ededed">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel
            Background="#1266a1" Grid.ColumnSpan="2">
            
        </StackPanel>

        <StackPanel
            Background="#1266a1" Grid.RowSpan="2" >

            <Button 
                Height="32px"
                Margin="10px"
                Content="Рабочий стол">

            </Button>

            <Button 
                Height="32px"
                Margin="10px"
                Content="Главная">

            </Button>

            <Button 
                Height="32px"
                Margin="10px"
                Content="Проекты">

            </Button>

            <Button 
                Height="32px"
                Margin="10px"
                Content="Услуги">

            </Button>

            <Button 
                Height="32px"
                Margin="10px"
                Content="Блог">

            </Button>

            <Button 
                Height="32px"
                Margin="10px"
                Content="Контакты">

            </Button>
        </StackPanel>
        <Grid Margin="185,33,10,10">
            
        </Grid>
    </Grid>
</Window>
